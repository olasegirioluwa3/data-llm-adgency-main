import functions as fc

# Specify the folder path and output file name
folder_path = 'output'
output_file = 'final/merged_output.csv'

# Call the function to merge CSV files
fc.merge_csv_files(folder_path, output_file)
